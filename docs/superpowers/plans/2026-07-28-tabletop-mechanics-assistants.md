# 面杀机制辅助实现计划

1. 新增纯规则模块，测试并实现：
   - 助手模块识别和标记建议提取
   - 标记台状态操作
   - 扶汉五将抽取和体力上限计算
   - 谋弈两段隐私流程与结果判断
   - 转换技及司马孚逐目标状态
2. 更新移动版数据生成脚本，把新增模块自动挂到武将并刷新现有 `heroes.json`。
3. 新增四类 React 助手，接入现有抽屉、重置、本地状态和多标签导航。
4. 补充桌面端和移动端样式、无障碍标签与技能规则提示。
5. 跑单元测试、数据审计、lint、生产构建和渲染测试。
6. 提交、推送 GitHub，并部署 Netlify 正式站点。
